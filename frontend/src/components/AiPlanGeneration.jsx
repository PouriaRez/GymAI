import React from 'react';

const AiPlanGeneration = () => {
  // First create the part where the user enters info
  //  and then show the AI's recommended workouts

  // useEffect(() => {
  //   fetch('http://localhost:3010/api/v0/ai/chat', {
  //     method: 'POST',
  //     headers: { 'Content-Type': 'application/json' },
  //     body: JSON.stringify(userInfo),
  //   })
  //     .then((res) => res.json())
  //     .then((data) => setPlan(data.result));
  //   console.log(plan);
  // }, []);

  return <div>AiPlanGeneration</div>;
};

export default AiPlanGeneration;
